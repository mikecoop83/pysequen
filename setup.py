from setuptools import setup

setup(name='pysequen',
      version='0.1',
      description='Python library for concurrent processing of tasks with dependencies requiring sequencing',
      url='https://github.com/mikecoop83/pysequen/',
      author='Michael Coopersmith',
      author_email='mikecoop83@gmail.com',
      license='MIT',
      packages=['pysequen'],
      install_requires=[],
      zip_safe=False)
